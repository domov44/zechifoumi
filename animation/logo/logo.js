// logo
const logo = document.getElementById("logo-container");

const logoOptions = {
  container: logo,
  renderer: "svg",
  loop: false,
  autoplay: true,
  path: "animation/logo/ra.json",
};

constlogo= lottie.loadAnimation(logoOptions);